---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import CommunitySettings from '../../../components/community/CommunitySettings.svelte';

const { name } = Astro.params;
---
<BaseLayout title={`r/${name} settings`}>
  <CommunitySettings client:load name={name!} />
</BaseLayout>
