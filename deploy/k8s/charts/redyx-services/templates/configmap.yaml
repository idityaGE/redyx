{{/*
ConfigMap template for all Redyx microservices
Contains environment variables per service
*/}}

{{- $root := . -}}
{{- range $serviceName, $serviceConfig := .Values.services }}
{{- if $serviceConfig.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $serviceName }}-service-config
  labels:
    {{- include "redyx-services.serviceLabels" $root | nindent 4 }}
    app.kubernetes.io/name: {{ $serviceName }}-service
    app.kubernetes.io/component: {{ $serviceName }}
data:
  {{- range $key, $value := $serviceConfig.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
{{- end }}

{{/*
Envoy ConfigMap with envoy.yaml configuration
Loaded from files/envoy-k8s.yaml
*/}}
{{- if .Values.envoy.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-config
  labels:
    {{- include "redyx-services.serviceLabels" . | nindent 4 }}
    app.kubernetes.io/name: envoy-gateway
    app.kubernetes.io/component: gateway
data:
  envoy.yaml: |-
{{ .Files.Get "files/envoy-k8s.yaml" | indent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-proto
  labels:
    {{- include "redyx-services.serviceLabels" . | nindent 4 }}
    app.kubernetes.io/name: envoy-gateway
    app.kubernetes.io/component: gateway
binaryData:
  proto.pb: {{ .Files.Get "files/proto.pb" | b64enc }}
{{- end }}
