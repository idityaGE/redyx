---
phase: 03-posts-voting-feeds
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - web/src/components/VoteButtons.svelte
  - web/src/components/FeedRow.svelte
  - web/src/components/FeedList.svelte
  - web/src/components/SortBar.svelte
  - web/src/components/PostBody.svelte
  - web/src/lib/time.ts
  - web/package.json
autonomous: true
requirements:
  - POST-04
  - POST-06
  - VOTE-03

must_haves:
  truths:
    - "Vote buttons show score with color-coded state (accent=up, red=down) and update optimistically on click"
    - "Feed rows display in compact terminal style: vote arrows | title | metadata"
    - "Link posts show domain tag, media posts show tiny thumbnail"
    - "Sort bar shows Hot/New/Top/Rising tabs with secondary time filter for Top"
    - "Feed list implements infinite scroll with cursor-based auto-loading"
    - "Markdown body renders safely with DOMPurify sanitization"
    - "Unauthenticated users see vote buttons but clicking prompts login"
    - "Scores above 999 display in compact format (1.4k, 15.8k)"
  artifacts:
    - path: "web/src/components/VoteButtons.svelte"
      provides: "Optimistic voting component with color states"
      min_lines: 50
    - path: "web/src/components/FeedRow.svelte"
      provides: "Compact terminal-style feed row"
      min_lines: 30
    - path: "web/src/components/FeedList.svelte"
      provides: "Infinite scroll feed container with cursor pagination"
      min_lines: 60
    - path: "web/src/components/SortBar.svelte"
      provides: "Sort tabs + time range filter"
      min_lines: 30
    - path: "web/src/components/PostBody.svelte"
      provides: "Markdown rendering with DOMPurify"
      min_lines: 20
    - path: "web/src/lib/time.ts"
      provides: "Relative time formatting (2h ago, 3d ago)"
      exports: ["relativeTime"]
  key_links:
    - from: "web/src/components/VoteButtons.svelte"
      to: "web/src/lib/api.ts"
      via: "api('/votes', { method: 'POST' })"
      pattern: "api.*votes"
    - from: "web/src/components/FeedList.svelte"
      to: "web/src/lib/api.ts"
      via: "api(endpoint) for feed data"
      pattern: "api.*endpoint"
    - from: "web/src/components/FeedRow.svelte"
      to: "web/src/components/VoteButtons.svelte"
      via: "VoteButtons component embedded in each row"
      pattern: "VoteButtons"
---

<objective>
Build all reusable frontend Svelte components for the posts + voting + feeds feature: VoteButtons (optimistic voting), FeedRow (compact terminal-style), FeedList (infinite scroll), SortBar (sort tabs + time filter), PostBody (markdown rendering), and time formatting utility.

Purpose: These components are the building blocks for all feed pages (home, community, saved) and the post detail page. Building them as reusable components in one plan ensures consistency and avoids duplication.
Output: 6 Svelte components + 1 utility module, ready to be assembled into pages in Plan 05.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-posts-voting-feeds/03-CONTEXT.md
@.planning/phases/03-posts-voting-feeds/03-RESEARCH.md

@web/src/lib/api.ts
@web/src/lib/auth.ts
@web/src/components/CommunityDetail.svelte
@web/src/pages/index.astro
@web/src/components/ProfileTabs.svelte

<interfaces>
<!-- API response shapes from proto definitions -->

Post object (camelCase JSON from Envoy transcoder):
```typescript
type Post = {
  postId: string;
  title: string;
  body: string;
  url: string;
  postType: 'POST_TYPE_TEXT' | 'POST_TYPE_LINK' | 'POST_TYPE_MEDIA';
  authorId: string;
  authorUsername: string;
  communityId: string;
  communityName: string;
  voteScore: number;
  commentCount: number;
  isEdited: boolean;
  isDeleted: boolean;
  isPinned: boolean;
  isAnonymous: boolean;
  mediaUrls: string[];
  thumbnailUrl: string;
  createdAt: string;  // ISO timestamp
  editedAt: string;
};
```

Feed API responses:
```typescript
type FeedResponse = {
  posts: Post[];
  pagination: { nextCursor: string; hasMore: boolean };
};
```

Vote API:
- POST /api/v1/votes { targetId, targetType, direction } → { newScore: number }
- direction: 'VOTE_DIRECTION_UP' | 'VOTE_DIRECTION_DOWN' | 'VOTE_DIRECTION_NONE'

From web/src/lib/api.ts:
- api<T>(path, options) — fetch wrapper with auth + 401 refresh
- ApiError class with status property

From web/src/lib/auth.ts:
- isAuthenticated() — checks login state
- getUser() — returns AuthUser | null
- subscribe(fn) — listen for auth changes

Svelte 5 patterns (from CommunityDetail.svelte):
- $state, $props, $derived for reactivity
- onMount for lifecycle
- client:load for interactive islands

Terminal styling classes (from index.astro + terminal.css):
- text-terminal-fg, text-terminal-dim, text-accent-500, text-accent-600
- bg-terminal-surface, border-terminal-border
- box-terminal, font-mono, text-xs, text-sm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install marked + DOMPurify, create time utility + VoteButtons + PostBody components</name>
  <files>
    web/package.json,
    web/src/lib/time.ts,
    web/src/components/VoteButtons.svelte,
    web/src/components/PostBody.svelte
  </files>
  <action>
    **1. Install npm dependencies** (run from web/ directory):
    ```bash
    npm install marked dompurify
    npm install -D @types/dompurify
    ```

    **2. Create `web/src/lib/time.ts`** — relative time formatting:
    ```typescript
    export function relativeTime(isoString: string): string {
      const now = Date.now();
      const then = new Date(isoString).getTime();
      const seconds = Math.floor((now - then) / 1000);
      
      if (seconds < 60) return 'just now';
      const minutes = Math.floor(seconds / 60);
      if (minutes < 60) return `${minutes}m`;
      const hours = Math.floor(minutes / 60);
      if (hours < 24) return `${hours}h`;
      const days = Math.floor(hours / 24);
      if (days < 30) return `${days}d`;
      const months = Math.floor(days / 30);
      if (months < 12) return `${months}mo`;
      const years = Math.floor(days / 365);
      return `${years}y`;
    }
    ```

    **3. Create `web/src/components/VoteButtons.svelte`**:
    Follow the RESEARCH.md pattern exactly (Optimistic Voting section):
    - Props: `postId: string`, `targetType: string` (default 'TARGET_TYPE_POST'), `initialScore: number`, `initialVote: number` (-1, 0, 1)
    - Local $state: `score`, `userVote`
    - `formatScore(n)`: exact up to 999, then compact:
      - n >= 1_000_000 → `(n/1_000_000).toFixed(1) + 'm'`
      - n >= 1_000 → `(n/1_000).toFixed(1) + 'k'`
      - else → n.toString()
      - Remove trailing `.0` (e.g., `1.0k` → `1k`)
    - `vote(direction: 'up' | 'down')`:
      - If `!isAuthenticated()` → `window.location.href = '/login'` (per CONTEXT: "prompt login on click")
      - Save prev state, compute new direction (toggle if same), compute optimistic score
      - Apply immediately to $state
      - Call `api<{ newScore: number }>('/votes', { method: 'POST', body: ... })`
      - On success: reconcile with `res.newScore`
      - On failure: rollback to prev state
    - Template: vertical layout, up arrow button, score text, down arrow button
    - Colors per CONTEXT decisions:
      - userVote === 1: arrow + score text get `text-accent-500` (teal/green)
      - userVote === -1: arrow + score text get `text-red-500`
      - else: `text-terminal-dim` for arrows, `text-terminal-fg` for score
    - Use `&#9650;` (▲) for up arrow, `&#9660;` (▼) for down arrow
    - Width: `w-10 shrink-0`, flex-col items-center

    **4. Create `web/src/components/PostBody.svelte`**:
    - Props: `body: string`
    - Use `marked` + `DOMPurify` for safe rendering
    - Configure marked: `{ breaks: true, gfm: true }` (no headerIds)
    - Use `$derived` to compute HTML: `DOMPurify.sanitize(marked.parse(body))`
    - Render with `{@html html}` inside a prose-styled div
    - Styling: `font-mono text-sm text-terminal-fg` for terminal aesthetic
    - Code blocks should use the existing terminal styling (bg-terminal-surface, border-terminal-border)
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>marked + DOMPurify installed. relativeTime utility formats timestamps. VoteButtons shows colored vote arrows with optimistic updates and login prompt for anon. PostBody renders markdown safely with terminal styling.</done>
</task>

<task type="auto">
  <name>Task 2: FeedRow, SortBar, and FeedList (infinite scroll) components</name>
  <files>
    web/src/components/FeedRow.svelte,
    web/src/components/SortBar.svelte,
    web/src/components/FeedList.svelte
  </files>
  <action>
    **1. Create `web/src/components/FeedRow.svelte`** — compact terminal-style row:
    Follow the RESEARCH.md FeedRow pattern, matching the index.astro mockup aesthetic.
    
    Props: `post: Post` (Post type as defined in interfaces above), `userVote?: number` (default 0)
    
    Layout (per CONTEXT: "vote arrows | title | metadata (community, author, time, comment count)"):
    ```
    [▲]  Show: I built a terminal Reddit client in Rust (github.com)
    [42]  r/programming · u/rustacean42 · 2h · 87 comments
    [▼]
    ```
    
    - Embed `<VoteButtons>` on the left with postId, initialScore=post.voteScore, initialVote=userVote
    - Title as link to `/post/{post.postId}`, truncated with `truncate` class
    - If `post.postType === 'POST_TYPE_LINK'`:
      - Extract domain from post.url: `new URL(post.url).hostname` (wrapped in try/catch)
      - Show domain tag: `(github.com)` in `text-terminal-dim text-xs` (per CONTEXT: "domain tags on link posts")
    - If `post.postType === 'POST_TYPE_MEDIA'` and `post.thumbnailUrl`:
      - Show 32px thumbnail at end of row (per CONTEXT: "tiny 32-40px thumbnail")
    - Metadata line: community link (`r/{communityName}` in accent-600), · separator, author (`u/{authorUsername}` or `[anonymous]`), · time (relativeTime), · comment count
    - If `post.isAnonymous`: show `[anonymous]` instead of username
    - Row styling: `flex items-center gap-3 px-2 py-1.5 border-b border-terminal-border hover:bg-terminal-surface transition-colors text-xs font-mono`
    - Group hover: title changes to accent-500

    **2. Create `web/src/components/SortBar.svelte`** — inline tab bar:
    Props: `sort: string` (current sort), `timeRange?: string` (current time range for Top), `onSortChange: (sort: string, timeRange?: string) => void`
    
    Per CONTEXT decisions:
    - "Inline horizontal tab bar above the feed: Hot | New | Top | Rising"
    - "When Top is selected, a secondary time filter row appears: Hour | Day | Week | Month | Year | All"
    
    Implementation:
    - Sort tabs: `[{id: 'SORT_ORDER_HOT', label: 'Hot'}, {id: 'SORT_ORDER_NEW', label: 'New'}, {id: 'SORT_ORDER_TOP', label: 'Top'}, {id: 'SORT_ORDER_RISING', label: 'Rising'}]`
    - Active tab: terminal style like ProfileTabs — `bg-terminal-bg text-accent-500 border-b-2 border-accent-500`
    - Inactive: `text-terminal-dim hover:text-terminal-fg`
    - When sort === 'SORT_ORDER_TOP': show secondary row with time range buttons:
      `[{id: 'TIME_RANGE_HOUR', label: 'Hour'}, ..., {id: 'TIME_RANGE_ALL', label: 'All'}]`
    - Default time range when switching to Top: 'TIME_RANGE_DAY'
    - On any click: call `onSortChange(newSort, newTimeRange)`
    - Styling: `border border-terminal-border bg-terminal-surface font-mono text-xs`

    **3. Create `web/src/components/FeedList.svelte`** — infinite scroll container:
    Follow the RESEARCH.md FeedList pattern exactly.
    
    Props: `endpoint: string`, `sort: string`, `timeRange?: string`
    
    Implementation:
    - Local $state: `posts: Post[]`, `nextCursor: string | null`, `hasMore: boolean`, `loading: boolean`, `initialLoading: boolean`
    - `sentinel` element (bound with `bind:this`) at bottom — 1px div
    - `loadPage()`:
      - Build query params: `pagination.limit=25`, optional `pagination.cursor`, `sort`, `timeRange`
      - Call `api<FeedResponse>(endpoint + '?' + params)`
      - Append to posts array (not replace)
      - Update cursor and hasMore from response pagination
    - `onMount()`:
      - Call loadPage() for initial load
      - Set up IntersectionObserver on sentinel with `rootMargin: '200px'`
      - When sentinel intersects and hasMore and !loading → call loadPage()
      - Return cleanup function to disconnect observer
    - `$effect()` to watch sort and timeRange props:
      - When sort or timeRange changes → reset posts to [], cursor to null, hasMore to true, call loadPage()
    - Template:
      - Initial loading: `[loading feed...]` in terminal dim, animate-pulse
      - Empty: `no posts yet` in terminal dim
      - Posts: `{#each posts as post (post.postId)} <FeedRow {post} /> {/each}`
      - Loading more: `[loading more...]` in terminal dim
      - Sentinel div at bottom
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>FeedRow renders compact terminal-style rows with vote buttons, domain tags, thumbnails, and metadata. SortBar shows Hot/New/Top/Rising tabs with secondary time filter for Top. FeedList implements infinite scroll with IntersectionObserver, cursor pagination, and sort/timeRange reactivity.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds without errors
- All 5 new Svelte components exist and import correctly
- VoteButtons handles all vote state transitions with color changes
- FeedList IntersectionObserver is properly cleaned up on unmount
- SortBar shows/hides time range filter based on Top selection
- PostBody uses DOMPurify (no raw HTML without sanitization)
</verification>

<success_criteria>
- VoteButtons: optimistic update, color states (accent/red), compact score format, login prompt for anon
- FeedRow: compact 1-2 line rows, domain tag for links, thumbnail for media, anonymous handling
- FeedList: infinite scroll via IntersectionObserver, cursor pagination, sort-reactive reset
- SortBar: 4 sort tabs, secondary time filter row for Top sort
- PostBody: safe markdown rendering with marked + DOMPurify
- All components use Svelte 5 runes ($state, $props, $derived, $effect)
- All components match terminal aesthetic (monospace, dim metadata, accent highlights)
</success_criteria>

<output>
After completion, create `.planning/phases/03-posts-voting-feeds/03-04-SUMMARY.md`
</output>
