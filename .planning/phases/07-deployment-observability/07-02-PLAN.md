---
phase: 07-deployment-observability
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - deploy/k8s/data/postgresql.yaml
  - deploy/k8s/data/redis.yaml
  - deploy/k8s/data/scylladb.yaml
  - deploy/k8s/data/kafka.yaml
  - deploy/k8s/data/meilisearch.yaml
  - deploy/k8s/data/minio.yaml
  - deploy/docker/init-databases.sql
  - Makefile
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "All 6 data stores are running and healthy in redyx-data namespace"
    - "PostgreSQL has all 8 databases created (skeleton, auth, user_profiles, community, posts_shard_0, posts_shard_1, notifications, moderation, media)"
    - "Services can connect to data stores via K8s DNS names"
  artifacts:
    - path: "deploy/k8s/data/postgresql.yaml"
      provides: "PostgreSQL Helm values"
      contains: "auth.postgresPassword"
    - path: "deploy/k8s/data/redis.yaml"
      provides: "Redis Helm values"
      contains: "architecture: standalone"
    - path: "deploy/k8s/data/kafka.yaml"
      provides: "Kafka Helm values"
      contains: "kraft.enabled"
  key_links:
    - from: "deploy/k8s/charts/redyx-services/values.yaml"
      to: "deploy/k8s/data/postgresql.yaml"
      via: "DATABASE_URL env var with K8s DNS"
      pattern: "postgres://.*\\.redyx-data\\.svc"
    - from: "Makefile"
      to: "deploy/k8s/data/*.yaml"
      via: "helm install commands"
      pattern: "helm.*-f deploy/k8s/data"
---

<objective>
Deploy all data stores to Kubernetes using Bitnami Helm charts: PostgreSQL, Redis, ScyllaDB, Kafka, Meilisearch, and MinIO.

Purpose: Provide stateful data layer for all 12 microservices with proper health checks and persistence.
Output: Helm values files for 6 data stores, updated Makefile target, working data layer in redyx-data namespace.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment-observability/07-CONTEXT.md
@.planning/phases/07-deployment-observability/07-RESEARCH.md
@.planning/phases/07-deployment-observability/07-01-SUMMARY.md
@docker-compose.yml
</context>

<interfaces>
<!-- Data store connection patterns from docker-compose.yml -->
PostgreSQL databases: skeleton, auth, user_profiles, community, posts_shard_0, posts_shard_1, notifications, moderation, media
PostgreSQL credentials: redyx/dev
Redis: single instance, databases 0-11 for service isolation
ScyllaDB: keyspace redyx_comments
Kafka: KRaft mode, bootstrap server kafka:9092
Meilisearch: master key dev-master-key
MinIO: credentials minioadmin/minioadmin, bucket redyx-media
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create PostgreSQL and Redis Helm values</name>
  <files>deploy/k8s/data/postgresql.yaml, deploy/k8s/data/redis.yaml, deploy/docker/init-databases.sql</files>
  <action>
**deploy/k8s/data/postgresql.yaml** (Bitnami PostgreSQL chart values):
```yaml
# bitnami/postgresql chart values
global:
  postgresql:
    auth:
      postgresPassword: "dev"
      username: "redyx"
      password: "dev"
      database: "skeleton"

primary:
  persistence:
    enabled: true
    size: 1Gi
  initdb:
    scriptsConfigMap: postgresql-init-scripts  # Created from init-databases.sql

service:
  type: ClusterIP
  ports:
    postgresql: 5432

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    namespace: redyx-monitoring
```

**deploy/k8s/data/redis.yaml** (Bitnami Redis chart values):
```yaml
# bitnami/redis chart values
architecture: standalone

auth:
  enabled: false  # Match docker-compose (no auth)

master:
  persistence:
    enabled: true
    size: 1Gi
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    namespace: redyx-monitoring
```

**Update deploy/docker/init-databases.sql** to include all 9 databases:
- Ensure it creates: skeleton, auth, user_profiles, community, posts_shard_0, posts_shard_1, notifications, moderation, media
- Each with GRANT ALL to redyx user

Create ConfigMap for PostgreSQL init:
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-init-scripts
  namespace: redyx-data
data:
  init-databases.sql: |
    (contents of init-databases.sql)
```
  </action>
  <verify>
    <automated>test -f deploy/k8s/data/postgresql.yaml && test -f deploy/k8s/data/redis.yaml && grep -q "posts_shard_0" deploy/docker/init-databases.sql</automated>
  </verify>
  <done>PostgreSQL values with init script configmap, Redis values with standalone mode, both with metrics enabled</done>
</task>

<task type="auto">
  <name>Task 2: Create ScyllaDB, Kafka, Meilisearch, MinIO Helm values</name>
  <files>deploy/k8s/data/scylladb.yaml, deploy/k8s/data/kafka.yaml, deploy/k8s/data/meilisearch.yaml, deploy/k8s/data/minio.yaml</files>
  <action>
**deploy/k8s/data/scylladb.yaml** (scylla-operator or bitnami/cassandra with ScyllaDB image):
```yaml
# Using bitnami/cassandra with ScyllaDB image override
# ScyllaDB operator is complex for local dev
image:
  registry: docker.io
  repository: scylladb/scylla
  tag: "6.2"

cluster:
  name: redyx-scylla

persistence:
  enabled: true
  size: 1Gi

resources:
  requests:
    cpu: 100m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

# Developer mode for local
extraEnvVars:
  - name: SCYLLA_DEVELOPER_MODE
    value: "1"

startupProbe:
  enabled: true
  initialDelaySeconds: 60
  periodSeconds: 10
  failureThreshold: 30

# Keyspace init handled by comment-service migrations
```

**deploy/k8s/data/kafka.yaml** (Bitnami Kafka chart):
```yaml
# bitnami/kafka chart values - KRaft mode (no Zookeeper)
kraft:
  enabled: true
  clusterId: "MkU3OEVBNTcwNTJENDM2Qk"

controller:
  replicaCount: 1

broker:
  replicaCount: 1

listeners:
  client:
    protocol: PLAINTEXT
  controller:
    protocol: PLAINTEXT

persistence:
  enabled: true
  size: 1Gi

resources:
  requests:
    cpu: 100m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

provisioning:
  enabled: true
  topics:
    - name: redyx.votes.v1
      partitions: 6
      replicationFactor: 1
    - name: redyx.posts.v1
      partitions: 3
      replicationFactor: 1
    - name: redyx.comments.v1
      partitions: 3
      replicationFactor: 1

metrics:
  kafka:
    enabled: true
  serviceMonitor:
    enabled: true
    namespace: redyx-monitoring
```

**deploy/k8s/data/meilisearch.yaml**:
```yaml
# meilisearch/meilisearch chart values
environment:
  MEILI_ENV: development
  MEILI_MASTER_KEY: "dev-master-key"

persistence:
  enabled: true
  size: 1Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
```

**deploy/k8s/data/minio.yaml** (Bitnami MinIO chart):
```yaml
# bitnami/minio chart values
auth:
  rootUser: minioadmin
  rootPassword: minioadmin

defaultBuckets: "redyx-media"

persistence:
  enabled: true
  size: 1Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

# Console access
service:
  type: ClusterIP
  ports:
    api: 9000
    console: 9001
```
  </action>
  <verify>
    <automated>test -f deploy/k8s/data/scylladb.yaml && test -f deploy/k8s/data/kafka.yaml && test -f deploy/k8s/data/meilisearch.yaml && test -f deploy/k8s/data/minio.yaml</automated>
  </verify>
  <done>All 4 data store value files exist with correct configurations matching docker-compose</done>
</task>

<task type="auto">
  <name>Task 3: Update Makefile k8s-data target with Helm commands</name>
  <files>Makefile</files>
  <action>
Update the k8s-data target placeholder from Plan 01 with actual Helm install commands:

```makefile
k8s-data:  ## Deploy data stores (PostgreSQL, Redis, ScyllaDB, Kafka, Meilisearch, MinIO)
	@echo "Adding Helm repos..."
	helm repo add bitnami https://charts.bitnami.com/bitnami
	helm repo add meilisearch https://meilisearch.github.io/meilisearch-kubernetes
	helm repo update
	@echo "Creating PostgreSQL init ConfigMap..."
	kubectl create configmap postgresql-init-scripts -n $(K8S_NAMESPACE_DATA) --from-file=init-databases.sql=deploy/docker/init-databases.sql --dry-run=client -o yaml | kubectl apply -f -
	@echo "Deploying PostgreSQL..."
	helm upgrade --install postgresql bitnami/postgresql -n $(K8S_NAMESPACE_DATA) -f deploy/k8s/data/postgresql.yaml --wait
	@echo "Deploying Redis..."
	helm upgrade --install redis bitnami/redis -n $(K8S_NAMESPACE_DATA) -f deploy/k8s/data/redis.yaml --wait
	@echo "Deploying ScyllaDB..."
	helm upgrade --install scylladb bitnami/cassandra -n $(K8S_NAMESPACE_DATA) -f deploy/k8s/data/scylladb.yaml --wait --timeout 5m
	@echo "Deploying Kafka..."
	helm upgrade --install kafka bitnami/kafka -n $(K8S_NAMESPACE_DATA) -f deploy/k8s/data/kafka.yaml --wait --timeout 5m
	@echo "Deploying Meilisearch..."
	helm upgrade --install meilisearch meilisearch/meilisearch -n $(K8S_NAMESPACE_DATA) -f deploy/k8s/data/meilisearch.yaml --wait
	@echo "Deploying MinIO..."
	helm upgrade --install minio bitnami/minio -n $(K8S_NAMESPACE_DATA) -f deploy/k8s/data/minio.yaml --wait
	@echo "Data stores deployed!"

k8s-data-down:  ## Uninstall data stores
	-helm uninstall postgresql -n $(K8S_NAMESPACE_DATA)
	-helm uninstall redis -n $(K8S_NAMESPACE_DATA)
	-helm uninstall scylladb -n $(K8S_NAMESPACE_DATA)
	-helm uninstall kafka -n $(K8S_NAMESPACE_DATA)
	-helm uninstall meilisearch -n $(K8S_NAMESPACE_DATA)
	-helm uninstall minio -n $(K8S_NAMESPACE_DATA)
```

Also update values.yaml in the services chart to use K8s DNS names for data stores:
- PostgreSQL: `postgresql.redyx-data.svc.cluster.local:5432`
- Redis: `redis-master.redyx-data.svc.cluster.local:6379`
- ScyllaDB: `scylladb.redyx-data.svc.cluster.local:9042`
- Kafka: `kafka.redyx-data.svc.cluster.local:9092`
- Meilisearch: `meilisearch.redyx-data.svc.cluster.local:7700`
- MinIO: `minio.redyx-data.svc.cluster.local:9000`
  </action>
  <verify>
    <automated>grep -q "helm.*postgresql" Makefile && grep -q "helm.*kafka" Makefile && grep -q "helm.*minio" Makefile</automated>
  </verify>
  <done>Makefile k8s-data target installs all 6 data stores via Helm with proper wait flags</done>
</task>

</tasks>

<verification>
- All 6 value files exist in deploy/k8s/data/
- `make k8s-data` command runs without errors (after cluster exists)
- PostgreSQL creates all 9 databases
- Redis is accessible without auth
- Kafka has 3 topics pre-created
</verification>

<success_criteria>
- Helm values for PostgreSQL, Redis, ScyllaDB, Kafka, Meilisearch, MinIO exist
- Database init script ConfigMap pattern established
- Makefile targets install/uninstall data layer
- K8s DNS names documented for service configuration
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-observability/07-02-SUMMARY.md`
</output>
