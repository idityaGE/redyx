---
phase: 02-auth-user-community
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - docker-compose.yml
  - deploy/envoy/envoy.yaml
autonomous: true
requirements: [AUTH-04, RATE-04]

must_haves:
  truths:
    - "Docker Compose brings up auth, user, and community services alongside existing infrastructure"
    - "Envoy routes /api/v1/auth/* to auth-service, /api/v1/users/* to user-service, /api/v1/communities/* to community-service"
    - "REST curl to /api/v1/auth/register transcodes to gRPC and returns JSON response"
    - "Rate-limited requests receive HTTP 429 with Retry-After header via Envoy transcoding"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full Docker Compose with auth, user, community services"
      contains: "auth-service"
    - path: "deploy/envoy/envoy.yaml"
      provides: "Envoy routing for 3 backend services"
      contains: "auth-service"
  key_links:
    - from: "deploy/envoy/envoy.yaml"
      to: "docker-compose.yml"
      via: "cluster names match service names"
      pattern: "auth-service|user-service|community-service"
    - from: "deploy/envoy/envoy.yaml"
      to: "deploy/envoy/proto.pb"
      via: "proto descriptor with all service definitions"
      pattern: "AuthService|UserService|CommunityService"
---

<objective>
Update Docker Compose and Envoy to register all Phase 2 services — auth, user, community — with correct routing, health checks, and gRPC transcoding.

Purpose: The three backend services need to be reachable via REST through the Envoy gateway. This plan wires everything together and verifies the full stack works end-to-end.
Output: Working Docker Compose stack with all Phase 2 services accessible via REST/JSON through Envoy.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-user-community/02-01-SUMMARY.md
@.planning/phases/02-auth-user-community/02-02-SUMMARY.md
@.planning/phases/02-auth-user-community/02-03-SUMMARY.md
@.planning/phases/02-auth-user-community/02-04-SUMMARY.md

<interfaces>
<!-- Current Docker Compose structure -->
Services: postgres (5432), redis (6379), skeleton-service (50051), envoy (8080/9901)

<!-- Current Envoy routing -->
Single catch-all route: /api/v1/ → skeleton-service
Transcoder services: [redyx.health.v1.HealthService]

<!-- Proto HTTP annotations for routing -->
Auth:      POST /api/v1/auth/{register,login,refresh,logout,verify-otp,reset-password,google}
User:      GET /api/v1/users/{username}, PATCH/DELETE /api/v1/users/me, GET /api/v1/users/{username}/{posts,comments}
Community: GET/POST /api/v1/communities, GET/PATCH /api/v1/communities/{name}, POST/DELETE /api/v1/communities/{name}/members, GET /api/v1/communities/{name}/members, POST /api/v1/communities/{name}/moderators, DELETE /api/v1/communities/{name}/moderators/{user_id}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth, user, community services to Docker Compose</name>
  <files>docker-compose.yml</files>
  <action>
    Add three new services to docker-compose.yml, following the existing skeleton-service pattern:

    **auth-service:**
    ```yaml
    auth-service:
      build:
        context: .
        dockerfile: deploy/docker/Dockerfile
        args:
          SERVICE: auth
      environment:
        DATABASE_URL: postgres://redyx:dev@postgres:5432/auth?sslmode=disable
        REDIS_URL: redis://redis:6379/1
        GRPC_PORT: "50052"
        JWT_SECRET: "dev-jwt-secret-32-bytes-minimum!!"
      ports: ["50052:50052"]
      depends_on:
        postgres:
          condition: service_healthy
        redis:
          condition: service_healthy
    ```
    Note: Redis DB 1 for auth (DB 0 is skeleton). JWT_SECRET must be at least 32 bytes for HS256.

    **user-service:**
    ```yaml
    user-service:
      build:
        context: .
        dockerfile: deploy/docker/Dockerfile
        args:
          SERVICE: user
      environment:
        DATABASE_URL: postgres://redyx:dev@postgres:5432/user_profiles?sslmode=disable
        REDIS_URL: redis://redis:6379/2
        GRPC_PORT: "50053"
        JWT_SECRET: "dev-jwt-secret-32-bytes-minimum!!"
      ports: ["50053:50053"]
      depends_on:
        postgres:
          condition: service_healthy
        redis:
          condition: service_healthy
    ```

    **community-service:**
    ```yaml
    community-service:
      build:
        context: .
        dockerfile: deploy/docker/Dockerfile
        args:
          SERVICE: community
      environment:
        DATABASE_URL: postgres://redyx:dev@postgres:5432/community?sslmode=disable
        REDIS_URL: redis://redis:6379/3
        GRPC_PORT: "50054"
        JWT_SECRET: "dev-jwt-secret-32-bytes-minimum!!"
      ports: ["50054:50054"]
      depends_on:
        postgres:
          condition: service_healthy
        redis:
          condition: service_healthy
    ```

    Also update the envoy service depends_on to include the new services:
    ```yaml
    envoy:
      depends_on:
        - skeleton-service
        - auth-service
        - user-service
        - community-service
    ```

    IMPORTANT: All services share the same JWT_SECRET so the auth interceptor on user-service and community-service can validate tokens issued by auth-service.
  </action>
  <verify>
    <automated>cd /home/idityage/github_repos/reddit && docker compose config --quiet</automated>
  </verify>
  <done>Docker Compose defines auth-service (50052), user-service (50053), community-service (50054) with correct DB URLs and shared JWT secret</done>
</task>

<task type="auto">
  <name>Task 2: Update Envoy routes and transcoder for Phase 2 services</name>
  <files>deploy/envoy/envoy.yaml</files>
  <action>
    Update `deploy/envoy/envoy.yaml` with:

    1. **Add route entries** (BEFORE the existing catch-all `/api/v1/` route — Envoy matches first match):
       ```yaml
       routes:
         - match: { prefix: "/api/v1/auth/" }
           route:
             cluster: auth-service
             timeout: 30s
         - match: { prefix: "/api/v1/users/" }
           route:
             cluster: user-service
             timeout: 30s
         - match: { prefix: "/api/v1/communities/" }
           route:
             cluster: community-service
             timeout: 30s
         - match: { prefix: "/api/v1/" }
           route:
             cluster: skeleton-service
             timeout: 30s
       ```

    2. **Add services to grpc_json_transcoder** services list:
       ```yaml
       services:
         - redyx.health.v1.HealthService
         - redyx.auth.v1.AuthService
         - redyx.user.v1.UserService
         - redyx.community.v1.CommunityService
         - redyx.ratelimit.v1.RateLimitService
       ```

    3. **Add cluster definitions** for each new service:
       ```yaml
       - name: auth-service
         type: STRICT_DNS
         lb_policy: ROUND_ROBIN
         typed_extension_protocol_options:
           envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
             "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
             explicit_http_config:
               http2_protocol_options: {}
         load_assignment:
           cluster_name: auth-service
           endpoints:
           - lb_endpoints:
             - endpoint:
                 address:
                   socket_address: { address: auth-service, port_value: 50052 }
       ```
       Repeat pattern for user-service (50053) and community-service (50054).

    4. **Add access-control-expose-headers** to CORS response headers:
       ```yaml
       - header:
           key: access-control-expose-headers
           value: "grpc-status, grpc-message, retry-after"
       ```
       This ensures the frontend can read the Retry-After header on 429 responses.
  </action>
  <verify>
    <automated>cd /home/idityage/github_repos/reddit && docker compose config --quiet && grep -c "auth-service\|user-service\|community-service" deploy/envoy/envoy.yaml</automated>
  </verify>
  <done>Envoy routes /api/v1/auth/* to auth-service, /api/v1/users/* to user-service, /api/v1/communities/* to community-service. All proto services registered in transcoder. Clusters defined with HTTP/2.</done>
</task>

</tasks>

<verification>
1. `docker compose config` — valid YAML with all services
2. Envoy config has route entries for auth, user, community before catch-all
3. All 3 new clusters defined with HTTP/2 options
4. Proto service names added to transcoder services list
5. `docker compose build` — all service images build successfully
</verification>

<success_criteria>
- Docker Compose starts all 3 new services connected to correct databases
- Envoy correctly routes auth/user/community requests to their respective services
- `curl -X POST localhost:8080/api/v1/auth/register -d '{"email":"test@test.com","username":"testuser","password":"password123"}' -H 'Content-Type: application/json'` returns a JSON response (not 404)
- Rate-limited requests receive HTTP 429 with retry-after in response
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-user-community/02-05-SUMMARY.md`
</output>
