---
phase: 02-auth-user-community
plan: 09
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - web/src/pages/communities/index.astro
  - web/src/components/CommunityList.svelte
  - web/src/pages/communities/create.astro
  - web/src/components/CommunityCreateForm.svelte
  - web/src/pages/community/[name].astro
  - web/src/components/CommunityDetail.svelte
  - web/src/components/CommunitySidebar.svelte
  - web/src/pages/community/[name]/settings.astro
  - web/src/components/CommunitySettings.svelte
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, COMM-05, COMM-06]

must_haves:
  truths:
    - "User can browse communities at /communities with sorting by member count, date, or activity"
    - "User can create a community at /communities/create with name, description, visibility, and rules"
    - "Community detail page at /community/{name} shows sidebar with description, rules, members, and join/leave button"
    - "Community settings at /community/{name}/settings lets owner/mods edit description, rules, visibility, and manage moderators"
    - "Join/leave button updates member count reactively"
    - "Community list displays in information-dense table/list format"
  artifacts:
    - path: "web/src/pages/communities/index.astro"
      provides: "Browse communities page"
      min_lines: 10
    - path: "web/src/pages/communities/create.astro"
      provides: "Create community page"
      min_lines: 10
    - path: "web/src/pages/community/[name].astro"
      provides: "Community detail page"
      min_lines: 10
    - path: "web/src/pages/community/[name]/settings.astro"
      provides: "Community settings page"
      min_lines: 10
  key_links:
    - from: "web/src/components/CommunityDetail.svelte"
      to: "web/src/lib/api.ts"
      via: "api('/communities/{name}') fetch + join/leave calls"
      pattern: "api.*communities"
    - from: "web/src/components/CommunitySettings.svelte"
      to: "web/src/lib/api.ts"
      via: "api PATCH/POST/DELETE for settings, moderators"
      pattern: "api.*communities.*moderators"
---

<objective>
Create all community frontend pages: browse communities, create community, community detail with sidebar, and community settings for moderators.

Purpose: Communities are the organizational unit for content. Users need to discover, create, join, and manage communities before any posting happens in Phase 3.
Output: 4 Astro pages with Svelte components for full community management UI.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-user-community/02-06-SUMMARY.md

<interfaces>
<!-- API endpoints for communities -->
GET /api/v1/communities → { communities: [...], pagination: {} }
POST /api/v1/communities → { name, description, visibility } → { community: { ... } }
GET /api/v1/communities/{name} → { community: { ... }, isMember, isModerator }
PATCH /api/v1/communities/{name} → { description?, rules?, visibility?, ... } → { community: { ... } }
POST /api/v1/communities/{name}/members → {}  (join)
DELETE /api/v1/communities/{name}/members → {}  (leave)
GET /api/v1/communities/{name}/members → { members: [...], pagination: {} }
POST /api/v1/communities/{name}/moderators → { userId } → {}
DELETE /api/v1/communities/{name}/moderators/{userId} → {}

<!-- Community proto types -->
Community: { communityId, name, description, rules: CommunityRule[], bannerUrl, iconUrl, visibility, memberCount, ownerId, createdAt }
CommunityRule: { title, description }
Member: { userId, username, role, joinedAt }
Visibility: PUBLIC=1, RESTRICTED=2, PRIVATE=3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create browse communities and community creation pages</name>
  <files>web/src/pages/communities/index.astro, web/src/components/CommunityList.svelte, web/src/pages/communities/create.astro, web/src/components/CommunityCreateForm.svelte</files>
  <action>
    1. **communities/index.astro** — Browse communities page:
       - Uses BaseLayout with title="Communities"
       - Renders `<CommunityList client:load />`

    2. **CommunityList.svelte** — Information-dense community list:
       - Per locked decision: list/table format, NOT cards or tiles. Should feel like `ls -la` output.
       - Sort controls at top: "sort: [members] [created] [activity]" — styled as terminal toggle buttons
       - Svelte 5 rune: `let sortBy = $state('members')`
       - Fetch from `api('/communities?pagination.limit=25')` on mount and when sort changes
       - Table-like rows for each community:
         ```
         r/{name}  {memberCount} members  {visibility}  created {date}
         ```
         Each row is a link to `/community/{name}`
       - Use monospace aligned columns for information density
       - Pagination: "next >" / "< prev" cursors at bottom, styled as terminal navigation
       - Dim text for metadata (member count, date), normal text for community name
       - If authenticated: show "create community" link at top → `/communities/create`
       - If not authenticated: show `> login required to create communities` hint

    3. **communities/create.astro** — Create community page:
       - Uses BaseLayout with title="Create Community"
       - Renders `<CommunityCreateForm client:load />`

    4. **CommunityCreateForm.svelte** — Single-page creation form (per locked decision: no wizard):
       - Fields on one page:
         * Name: text input with validation feedback (3-21 chars, alphanumeric+underscore), show regex pattern as hint
         * Description: textarea (markdown supported hint)
         * Visibility: radio/select group — Public / Restricted / Private, each with one-line description
         * Rules: dynamic list — "add rule" button adds a row with title+description fields. "Remove" button per rule. Reorder not needed for Phase 2 (Claude's discretion).
       - Terminal aesthetic: field labels as `name: `, `description: `, `visibility: `, `rules: `
       - On submit: POST to `/api/v1/communities` with {name, description, visibility}
       - Note: Rules are sent via UpdateCommunity after creation (CreateCommunityRequest doesn't include rules in the proto). So: create community first, then if rules are entered, immediately call PATCH to add rules.
       - On success: redirect to `/community/{name}`
       - On error: inline terminal error messages
       - Requires authentication — if not logged in, redirect to `/login`
  </action>
  <verify>
    <automated>cd /home/idityage/github_repos/reddit/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Browse page shows communities in information-dense table format with sorting. Create page has single-form with name, description, visibility, rules.</done>
</task>

<task type="auto">
  <name>Task 2: Create community detail and settings pages</name>
  <files>web/src/pages/community/[name].astro, web/src/components/CommunityDetail.svelte, web/src/components/CommunitySidebar.svelte, web/src/pages/community/[name]/settings.astro, web/src/components/CommunitySettings.svelte</files>
  <action>
    1. **community/[name].astro** — Community detail page:
       - Uses BaseLayout with title="r/{name}"
       - Extract name from `Astro.params.name`
       - Renders `<CommunityDetail client:load name={name} />`

    2. **CommunityDetail.svelte** — Main community page layout:
       - Two-column layout: main content area (left) + CommunitySidebar (right)
       - Main content area: "posts feed placeholder — posts coming in Phase 3" message in dim text
       - Fetch community data from `api('/communities/' + name)`
       - Pass community data, isMember, isModerator to CommunitySidebar
       - If community not found (404): show "community not found" error page
       - If private and not member (403): show "this community is private — invitation required"

    3. **CommunitySidebar.svelte** — Right sidebar info panel (per locked decision):
       - Box-drawing border around sidebar content (per terminal aesthetic)
       - Community name: `r/{name}` in bold
       - Description: rendered as text (markdown rendering optional for Phase 2)
       - Rules: numbered list (1. Title — description)
       - Member count: `{count} members`
       - Mod list: list of moderator usernames (from members endpoint filtered by role, or from a separate call)
       - Join/Leave button:
         * If not authenticated: `> login to join` link
         * If not member: `[join]` button — calls POST `/api/v1/communities/{name}/members`, updates member count reactively
         * If member: `[leave]` button — calls DELETE `/api/v1/communities/{name}/members`
         * If owner: button disabled with `(owner)` label
       - Settings link: if isModerator, show `[settings]` link to `/community/{name}/settings`
       - All styled with terminal aesthetic: box-drawing borders, monospace, dim labels

    4. **community/[name]/settings.astro** — Settings page:
       - Uses BaseLayout with title="r/{name} settings"
       - Renders `<CommunitySettings client:load name={name} />`

    5. **CommunitySettings.svelte** — Moderator-only settings (per locked decision: dedicated settings page):
       - Check isModerator on mount — if not, redirect to community page with error
       - Sections (per locked decision):
         * **Description:** textarea, save button
         * **Rules:** dynamic list — add/remove/edit rules. Each rule has title + description fields. Reorder with up/down controls (or simple index-based reordering)
         * **Visibility:** radio group (Public/Restricted/Private), save button
         * **Moderators:** (owner only)
           - List current moderators with "revoke" button next to each (except owner)
           - "Add moderator" input: username field + submit. Calls POST `/api/v1/communities/{name}/moderators` with userId (need to look up user by username first — or accept userId directly if known)
           - For Phase 2 simplicity: accept username in the input, look up user profile via GET `/api/v1/users/{username}` to get userId, then call AssignModerator
       - Save buttons per section (not one big form — per locked decision each section is independent)
       - All API calls: PATCH `/api/v1/communities/{name}` for description/rules/visibility, POST/DELETE for moderators
  </action>
  <verify>
    <automated>cd /home/idityage/github_repos/reddit/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Community detail shows sidebar with info + join/leave. Settings page lets mods edit description, rules, visibility, and manage moderators.</done>
</task>

</tasks>

<verification>
1. `npm run build` — all pages compile
2. /communities route shows browsable list
3. /communities/create has single-page form
4. /community/{name} shows sidebar with join/leave
5. /community/{name}/settings shows mod-only controls
</verification>

<success_criteria>
- Browse page: information-dense list with sorting (COMM-01 discovery)
- Create page: single form with name validation, description, visibility, rules (COMM-01, COMM-02, COMM-03)
- Detail page: sidebar shows description, rules, members, join/leave (COMM-02, COMM-04)
- Settings: description editing, rules management, visibility toggle (COMM-02, COMM-03)
- Settings: moderator assignment/revocation by owner (COMM-05, COMM-06)
- All pages use terminal aesthetic with box-drawing borders
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-user-community/02-09-SUMMARY.md`
</output>
