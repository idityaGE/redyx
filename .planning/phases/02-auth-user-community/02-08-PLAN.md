---
phase: 02-auth-user-community
plan: 08
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified:
  - web/src/pages/user/[username].astro
  - web/src/components/ProfileHeader.svelte
  - web/src/components/ProfileTabs.svelte
  - web/src/components/ProfileEditor.svelte
autonomous: true
requirements: [USER-01, USER-02, USER-03, USER-04, USER-05]

must_haves:
  truths:
    - "Any user can view a profile page at /user/{username}"
    - "Profile shows compact status line: u/username | karma | joined date"
    - "Profile has tabbed sections: Posts | Comments | About"
    - "Authenticated user viewing own profile can inline-edit display name, bio, and avatar URL"
    - "Avatar displays as small square with ASCII box border, falls back to first letter"
    - "Account deletion is accessible from own profile with confirmation"
  artifacts:
    - path: "web/src/pages/user/[username].astro"
      provides: "Dynamic profile page route"
      min_lines: 10
    - path: "web/src/components/ProfileHeader.svelte"
      provides: "Compact profile status line with avatar"
      min_lines: 30
    - path: "web/src/components/ProfileTabs.svelte"
      provides: "Posts | Comments | About tab navigation and content"
      min_lines: 40
    - path: "web/src/components/ProfileEditor.svelte"
      provides: "Inline edit controls for own profile"
      min_lines: 30
  key_links:
    - from: "web/src/pages/user/[username].astro"
      to: "web/src/components/ProfileHeader.svelte"
      via: "client:load with username prop"
      pattern: "ProfileHeader.*client:load"
    - from: "web/src/components/ProfileHeader.svelte"
      to: "web/src/lib/api.ts"
      via: "api('/users/{username}') fetch"
      pattern: "api.*users"
---

<objective>
Create the user profile page with compact status line, tabbed content sections, inline editing for own profile, and account deletion.

Purpose: User profiles are a core social feature — users view each other's activity and manage their own identity. The profile page is referenced from posts, comments, and community member lists.
Output: Profile page at /user/{username} with header, tabs, and inline editing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-user-community/02-06-SUMMARY.md

<interfaces>
<!-- API endpoints for user profiles -->
GET /api/v1/users/{username} → { user: { userId, username, displayName, bio, avatarUrl, karma, createdAt } }
PATCH /api/v1/users/me → { displayName?, bio?, avatarUrl? } → { user: { ... } }
DELETE /api/v1/users/me → {} (requires auth)
GET /api/v1/users/{username}/posts → { posts: [], pagination: {} }  (empty in Phase 2)
GET /api/v1/users/{username}/comments → { comments: [], pagination: {} }  (empty in Phase 2)

<!-- Auth store -->
From web/src/lib/auth.ts:
- getUser() → { userId, username } | null
- isAuthenticated() → boolean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile page with header, tabs, and inline editing</name>
  <files>web/src/pages/user/[username].astro, web/src/components/ProfileHeader.svelte, web/src/components/ProfileTabs.svelte, web/src/components/ProfileEditor.svelte</files>
  <action>
    1. **[username].astro** — Dynamic route page:
       - Uses BaseLayout with title="u/{username}"
       - Extract `username` from `Astro.params.username`
       - Render ProfileHeader and ProfileTabs Svelte components with `client:load` and `username` prop

    2. **ProfileHeader.svelte** — Compact profile status line:
       - Props: `username: string`
       - On mount: fetch profile data from `api('/users/' + username)`
       - Display compact status line per locked decision: `u/{username} | {karma} karma | joined {date}`
       - Format date as "joined 2026-01-15" (YYYY-MM-DD, terminal-dense)
       - Avatar: small square (32-48px) with ASCII box-drawing border `┌─┐│ │└─┘` around it. If avatarUrl exists, show image. If not, show first letter of username in monospace, centered in the box.
       - If current user is viewing own profile: show "edit" indicator or ProfileEditor component
       - Check `getUser()?.username === username` to determine if own profile

    3. **ProfileTabs.svelte** — Tabbed content sections:
       - Props: `username: string`
       - Three tabs: Posts | Comments | About
       - Tab navigation styled like terminal multiplexer tabs (per locked decision):
         ```
         [Posts] [Comments] [About]
         ```
         Active tab has different styling (e.g., inverted colors or underline)
       - Svelte 5 rune: `let activeTab = $state('posts')`
       - **Posts tab:** Fetch from `api('/users/' + username + '/posts')`. In Phase 2 returns empty list → show "no posts yet" in dim terminal text
       - **Comments tab:** Fetch from `api('/users/' + username + '/comments')`. Same empty state.
       - **About tab:** Show display_name, bio (rendered as markdown — use a simple markdown renderer or just render as preformatted text for Phase 2). If own profile: show ProfileEditor inline.

    4. **ProfileEditor.svelte** — Inline edit controls:
       - Only shown when viewing own profile (checked by parent)
       - Click-to-edit pattern: display name, bio, avatar URL each have an "edit" toggle
       - On edit: field becomes an input/textarea (bio: textarea maxlength=500 with character counter)
       - On save: PATCH to `/api/v1/users/me` with changed fields
       - On success: update displayed values
       - On error: show inline terminal error
       - Avatar URL: text input (full upload flow in Phase 5 per research open question 2)
       - Account deletion: "delete account" link at bottom, styled as dangerous terminal command `> rm -rf account`
       - Confirmation step: shows "type 'delete' to confirm" pattern (Claude's discretion on exact UX)
       - On confirmed delete: DELETE `/api/v1/users/me`, logout, redirect to `/`
  </action>
  <verify>
    <automated>cd /home/idityage/github_repos/reddit/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Profile page shows status line, tabbed content, inline editing for own profile. Posts/Comments tabs show empty state. About tab shows bio with edit controls. Account deletion works with confirmation.</done>
</task>

</tasks>

<verification>
1. `npm run build` — compiles successfully
2. /user/[username] route renders profile page
3. Own profile shows edit controls, other profiles show read-only
4. Tabs switch between Posts, Comments, About content
5. Profile data fetched from API on mount
</verification>

<success_criteria>
- Profile page displays username, karma, cake day (USER-01)
- Posts/Comments tabs exist with pagination structure (USER-02 ready, empty for Phase 2)
- Karma shown in status line (USER-03 — displays value from API)
- Inline editing works for display name, bio (500 char limit), avatar URL (USER-04)
- Account deletion with confirmation flow (USER-05)
- Compact status line matches locked decision format
- Terminal multiplexer tab styling for content sections
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-user-community/02-08-SUMMARY.md`
</output>
